# heap-demo
A Python-based interactive demo of min-heap data structures. Includes array and tree visualizations, heapify, insert, delete, and extract-min operations, with optional verbose tracing to illustrate how the heap invariant is maintained.
